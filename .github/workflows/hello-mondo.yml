name: hello-mondo

on: push

jobs:
  hello-mondo-job:
    
    runs-on: ubuntu-latest

    steps:
      - name: check out repository code
        uses: actions/checkout@v4
      - run: echo "$(cat hello_mondo.txt)"
